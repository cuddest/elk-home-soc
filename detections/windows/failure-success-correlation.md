# Authentication failure -> success

**Signal:** same source produces >=3 authentication failures and >=1 success in ten minutes.

This is intentionally a correlation signal rather than proof of compromise. Analysts must validate the user, host, source, and business context.
